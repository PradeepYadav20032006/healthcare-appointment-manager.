# Base URL of the backend API (include /api). During local dev with the
# Vite proxy you can leave this unset and requests to /api/* are proxied
# to http://localhost:4000 automatically (see vite.config.js).
VITE_API_BASE_URL=http://localhost:4000/api
